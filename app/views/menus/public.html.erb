<div class="container mt-4">
  <div class="d-flex flex-column justify-content-center align-items-center mb-4 gap-3 text-center">
    <div>
      <h1><%= @menu.name %></h1>
      <p class="text-muted"><%= @menu.description %></p>
    </div>
  </div>

  <% if @sections.any? %>
    <% @sections.each do |section| %>
      <div class="card mb-4">
        <div class="card-header">
          <h3 class="mb-0"><%= section.name %></h3>
        </div>
        <div class="card-body">
          <% if section.description.present? %>
            <p class="text-muted"><%= section.description %></p>
          <% end %>

          <% if section.menu_items.any? %>
            <div class="menu-items-container">
              <% section.menu_items.each do |item| %>
                <div class="menu-item-card">
                  <div class="menu-item-image">
                    <%= image_tag "https://images.unsplash.com/photo-1546069901-ba9599a7e63c?w=200&h=200&fit=crop", alt: item.name, class: "item-img" %>
                  </div>
                  <div class="menu-item-content">
                    <div class="menu-item-header">
                      <h5 class="menu-item-name"><%= item.name %></h5>
                      <% if item.price.present? %>
                        <span class="menu-item-price">$<%= number_with_precision(item.price, precision: 2) %></span>
                      <% end %>
                    </div>
                    <% if item.description.present? %>
                      <p class="menu-item-description"><%= item.description %></p>
                    <% end %>
                  </div>
                </div>
              <% end %>
            </div>
          <% else %>
            <p class="text-muted fst-italic">No items in this section yet.</p>
          <% end %>
        </div>
      </div>
    <% end %>
  <% else %>
    <div class="alert alert-info">
      <p class="mb-0">This menu is currently empty.</p>
    </div>
  <% end %>
</div>
